import Vue from 'vue';

// 全局祖册vantui组件
import { 
    Button,
    NavBar,
    Icon,
    Popup,
    Field,
    Cell, CellGroup,
    Row, Col,Switch 
} from 'vant'
Vue.use(Button)
.use(NavBar)
.use(Icon)
.use(Popup)
.use(Field)
.use(Cell)
.use(CellGroup)
.use(Row)
.use(Col)
.use(Switch )
export default {
   
}